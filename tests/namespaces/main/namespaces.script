import dep -> deplib;
import testlib -> *;

namespace Internal {
    function immaSecret() {
        return "wheeeeee";
    }
}

function main(args) {
    v = Internal.immaSecret();
    assertStrEqual('wheeeeee', v);
    print('ALL DONE');
}
