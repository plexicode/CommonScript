value = args[0];
if (value.type != Type.STRING) {
    return ThrowErrorImpl(
        task,
        ExceptionTypeId.INVALID_ARGUMENT,
        "string.startsWith(value) requires a string argument");
}
int1 = string_findIndexOfSubString(
    (StringImpl) fp.ctx.internalValue,
    (StringImpl) value.internalValue,
    true, // forward
    true); // do only one check and then give up instead of searching the whole string.

if (int1 == -1) output = VALUE_FALSE;
else output = VALUE_TRUE;
